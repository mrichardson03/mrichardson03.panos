---
- import_tasks: 'reset.yml'

- name: test_panos_snippet_group
  vars:
    banner_text: new banner
  panos_snippet_group:
    src: 'iron-skillet_v90.yaml'
  register: result

- name: debug_result
  debug: msg={{ result }}

- name: test_panos_snippet_group - Assert create was successful
  assert:
    that:
      - result is success
      - result is changed

